# Components

Sonata consists of several key components: the chain (consensus and state), storage layer (encrypted files), metadata pipeline (DDEX), and client interfaces.

## Overview

This page describes each major component and its role in the Sonata system.

## Future Content

- Chain module breakdown
- Storage subsystem
- DDEX ingestion pipeline
- Client SDK and APIs

